export interface Prime {
   
    bankName:string , // Ensure this field is correctly populated before sending the request
    indexNumber: number,
    spread: number,
    floor:number,
    rateId: number
      // Add other properties as needed

     


    }